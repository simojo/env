#!/usr/bin/env sh

run() {
  rofi -show window \
    -matching fuzzy \
    -display-window ">" \
    -tokenize \
    -config "/env/dotfiles/rofi.rasi" \
    -disable-history \
    -hide-scrollbar
}

run
